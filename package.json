{
  "name": "eden",
  "version": "0.1.0",
  "description": "RBX Internal Developer Platform CLI",
  "bin": {
    "eden": "./src/index.ts"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --compile --outfile eden",
    "dev:api": "bun run src/api/server.ts",
    "build:api": "bun build src/api/server.ts --compile --outfile eden-api"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1048.0",
    "@clack/prompts": "^0.9.1",
    "hono": "^4.12.19",
    "yaml": "^2.4.5"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
